[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "oab-agents-control"
version = "0.1.0"
description = "Local-first control primitives for an OpenAB multi-agent catalog"
requires-python = ">=3.11"
dependencies = ["PyYAML>=6.0"]

[project.scripts]
oab-control = "oab_control.cli:main"

[tool.setuptools.packages.find]
include = ["oab_control*"]
